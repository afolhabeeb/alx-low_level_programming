#include "main.h"

/**
 * add - add two integer and return result
 * @a: first integer
 * @b: second integer
 * Return: always (0)
 */

int add(int a, int b)
{
	int result = a + b;

	return (result);
}
