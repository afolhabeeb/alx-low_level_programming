#include "main.h"

/**
 * set_string - sets value of a pointer to a character
 *
 * @s: character one
 * @to: string to source
 *
 * Return: print
 */

void set_string(char **s, char *to)
{
	*s = to;
}
