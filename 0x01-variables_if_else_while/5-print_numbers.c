#include <stdio.h>

/**
 * main - Program entry point
 *
 * Return: Always 0, program end point
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
		printf("%d", i);
	putchar('\n');

	return (0);

}
